--- a/partition.cpp
+++ b/partition.cpp
@@ -686,6 +686,25 @@
 		Setup_Data_Partition(Display_Error);
 	else if (Mount_Point == "/cache")
 		Setup_Cache_Partition(Display_Error);
+
+#ifdef TW_BACKUP_EXCLUSIONS
+	// backup_exclusions is a per-partition member, and the TW_BACKUP_EXCLUSIONS
+	// list is otherwise only read in Setup_Data_Media(), which runs for /data
+	// alone. /data already excludes /data/media wholesale, so board-defined
+	// exclusions could never affect anything. Apply them to every partition
+	// here instead, which is what a board-level exclusion list implies -- and
+	// what makes them reach an internal-storage mount point such as /storage.
+	{
+		std::vector<std::string> board_exclusions = TWFunc::Split_String(TW_BACKUP_EXCLUSIONS, ",");
+		for (const std::string& excl : board_exclusions) {
+			std::string trimmed = TWFunc::trim(excl);
+			if (!trimmed.empty()) {
+				backup_exclusions.add_absolute_dir(trimmed);
+				LOGINFO("Board exclusion '%s' added to partition '%s'\n", trimmed.c_str(), Mount_Point.c_str());
+			}
+		}
+	}
+#endif
 }
 
 void TWPartition::ExcludeAll(const string& path) {
--- a/partitionmanager.cpp
+++ b/partitionmanager.cpp
@@ -1150,14 +1150,17 @@
 		return true;
 	}
 backup_error:
-	Clean_Backup_Folder(part_settings->Backup_Folder);
+	// Save the log before cleaning, not after: cleaning now removes the folder
+	// itself, and copying into a folder that is already gone only produced a
+	// "No such file or directory" error on screen.
 	TWFunc::copy_file("/tmp/recovery.log", backup_log, 0644);
 	tw_set_default_metadata(backup_log.c_str());
+	Clean_Backup_Folder(part_settings->Backup_Folder, stop_backup.get_value() != 0);
 	TWFunc::SetPerformanceMode(false);
 	return false;
 }
 
-void TWPartitionManager::Clean_Backup_Folder(string Backup_Folder) {
+void TWPartitionManager::Clean_Backup_Folder(string Backup_Folder, bool Remove_Log) {
 	DIR *d = opendir(Backup_Folder.c_str());
 	struct dirent *p;
 	int r;
@@ -1168,6 +1171,10 @@
 	ext.push_back("md5");
 	ext.push_back("sha2");
 	ext.push_back("info");
+	// The saved log is worth keeping when the backup failed on its own, and
+	// only gets in the way when the user cancelled deliberately.
+	if (Remove_Log)
+		ext.push_back("log");
 
 	gui_msg("backup_clean=Backup Failed. Cleaning Backup Folder.");
 
@@ -1184,7 +1191,11 @@
 
 		size_t dot = path.find_last_of(".") + 1;
 		for (vector<string>::const_iterator i = ext.begin(); i != ext.end(); ++i) {
-			if (path.substr(dot) == *i) {
+			// Match on the start of the extension, not the whole of it: a
+			// backup that was split into several archives ends in win000,
+			// win001 and so on, none of which equal "win", so an exact
+			// comparison left every part of a split backup behind.
+			if (path.compare(dot, i->length(), *i) == 0) {
 				r = unlink(path.c_str());
 				if (r != 0)
 					LOGINFO("Unable to unlink '%s: %s'\n", path.c_str(), strerror(errno));
@@ -1192,6 +1203,11 @@
 		}
 	}
 	closedir(d);
+
+	// Drop the now-empty folder too, so a failed backup leaves nothing at all.
+	// rmdir refuses a non-empty directory, so anything unexpected is kept.
+	if (rmdir(Backup_Folder.c_str()) != 0)
+		LOGINFO("Backup folder '%s' not removed: %s\n", Backup_Folder.c_str(), strerror(errno));
 }
 
 int TWPartitionManager::Check_Backup_Cancel() {
@@ -1375,9 +1391,18 @@
         	{ 
           	   if (strstr(part_settings.Backup_Folder.c_str(), "data/media/0"))
              		{
+#ifdef TW_ALLOW_INTERNAL_SELF_BACKUP
+                	   // The board opts into this, having excluded the backup
+                	   // destination via TW_BACKUP_EXCLUSIONS. tar still reads a
+                	   // file that is growing underneath it if that exclusion is
+                	   // wrong or missing, so the destination folder must be
+                	   // covered there before enabling this.
+                	   gui_msg("fox_internal_q1=OrangeFox - Internal Storage - take care!");
+#else
                 	   gui_err("fox_internal_fatal_i1=OrangeFox: FATAL ERROR! You cannot backup Internal Storage onto itself!");
                 	   gui_err("fox_internal_fatal_i2=You MUST change the backup destination to MicroSD/USB-OTG.");
                 	   return false;
+#endif
              		} 
              		   else gui_msg("fox_internal_q1=OrangeFox - Internal Storage - take care!");
         	}
--- a/partitions.hpp
+++ b/partitions.hpp
@@ -400,7 +400,7 @@
 	TWPartition *Get_Default_Storage_Partition();                             // Returns a pointer to a default storage partition
 	int Check_Backup_Cancel();                                                // Returns the value of stop_backup
 	int Cancel_Backup();                                                      // Signals partition backup to cancel
-	void Clean_Backup_Folder(string Backup_Folder);                           // Clean Backup Folder on Error
+	void Clean_Backup_Folder(string Backup_Folder, bool Remove_Log = true);   // Clean Backup Folder on Error
 	int Fix_Contexts();
 	void Get_Partition_List(string ListType, std::vector<PartitionList> *Partition_List);
 	int Fstab_Processed();                                                    // Indicates if the fstab has been processed or not
--- a/Android.mk
+++ b/Android.mk
@@ -458,6 +458,9 @@
 ifneq ($(TW_BACKUP_EXCLUSIONS),)
     LOCAL_CFLAGS += -DTW_BACKUP_EXCLUSIONS='"$(TW_BACKUP_EXCLUSIONS)"'
 endif
+ifeq ($(TW_ALLOW_INTERNAL_SELF_BACKUP), true)
+    LOCAL_CFLAGS += -DTW_ALLOW_INTERNAL_SELF_BACKUP
+endif
 ifeq ($(TW_INCLUDE_FASTBOOTD), true)
     LOCAL_CFLAGS += -DTW_INCLUDE_FASTBOOTD
 endif
--- a/twrp-functions.cpp
+++ b/twrp-functions.cpp
@@ -1214,7 +1214,11 @@
   if (d)
     {
       struct dirent *p;
-      while (!r && (p = readdir(d)))
+      int failed = 0;
+      // Keep going after a failure instead of stopping at the first one: a
+      // cancelled backup leaves the archive the killed tar was still holding,
+      // and aborting there left every remaining file of that backup on disk.
+      while ((p = readdir(d)))
 	{
 	  if (!strcmp(p->d_name, ".") || !strcmp(p->d_name, ".."))
 	    continue;
@@ -1242,8 +1246,11 @@
 			  strerror(errno));
 		}
 	    }
+	  if (r)
+	    failed = r;
 	}
       closedir(d);
+      r = failed;
 
       if (!r)
 	{
